# ==============================================
# AuditPlus v2.0 - Variáveis de Ambiente
# ==============================================
# Copie este arquivo para .env e configure os valores
# NÃO comite o arquivo .env no repositório!

# ======== Banco de Dados ========
# Caminho do banco SQLite (deixe vazio para usar padrão)
DATABASE_PATH=audit_plus.db

# ======== Segurança ========
# Chave secreta para criptografia (gere uma nova para produção!)
SECRET_KEY=sua-chave-secreta-aqui-mude-em-producao

# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ======== Configurações de Aplicação ========
# Modo debug (True/False)
DEBUG_MODE=False

# Caminho para logs
LOG_PATH=logs/

# Tempo limite de sessão em minutos (0 = sem limite)
SESSION_TIMEOUT=30

# ======== Configurações de Backup ========
# Habilitar backup automático (True/False)
AUTO_BACKUP=True

# Intervalo de backup em horas
BACKUP_INTERVAL_HOURS=24

# Caminho para backups
BACKUP_PATH=backups/

# Número máximo de backups a manter
MAX_BACKUPS=7

# ======== Email (Opcional - para notificações) ========
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=auditplus@example.com



# ======== Integração (Opcional) ========
# API_KEY=
# API_URL=
